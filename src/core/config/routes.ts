import { AppRoute } from "@/models/general";


const routes: AppRoute[] = [
    {
        name: 'Notas',
        route: 'notes'
    },
    {
        name: 'Pendientes',
        route: 'tasks'
    }
];

export default routes;